import { useWindowSize } from '../../hooks/use-window-size';

export const ForgotPasswordPage = () => {
  const { width = 0 } = useWindowSize();

  return <form data-test-id='send-email-form'>bhbh</form>;
};
